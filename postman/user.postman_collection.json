{
  "info": {
    "name": "KairoX \u00b7 User",
    "description": "Login and the logins themselves. START HERE: run login, paste access_token into the collection's `token` variable, and every other collection is authorised.\n\nSETUP\n  1. Import the whole folder and SELECT the KairoX environment\n     (top-right). That is what shares one token across every\n     collection \u2014 a collection variable is visible only to the\n     collection that set it.\n  2. Run `user` \u2192 Login. Its test script stores `access_token` for\n     you; there is nothing to copy and paste.\n  3. Every request here inherits bearer auth from that token.\n\n`base_url` defaults to http://127.0.0.1:8000.\n\nOptional query params are present but DISABLED, so each request runs\nas-is. File uploads are real form-data rows \u2014 click Select Files on\nthe `file` row. Generated from the app's own OpenAPI schema by\n`python scripts/make_postman_per_service.py` \u2014 re-run it after changing\nroutes rather than editing requests by hand.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{token}}",
        "type": "string"
      }
    ]
  },
  "variable": [
    {
      "key": "base_url",
      "value": "http://127.0.0.1:8000",
      "type": "string"
    },
    {
      "key": "token",
      "value": "",
      "type": "string"
    }
  ],
  "item": [
    {
      "name": "Authentication",
      "item": [
        {
          "name": "Change Password",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/api/v1/auth/change-password",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "change-password"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"current_password\": \"\",\n  \"new_password\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "Login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/api/v1/auth/login",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "login"
              ]
            },
            "description": "Sign in. THROTTLED PER IP (core/throttle.py) \u2014 this is the only route an\nattacker can reach without credentials, and bcrypt at cost 12 makes a\nsustained guessing run a denial of service as well as a security problem.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"username\": \"\",\n  \"password\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Stores access_token so you never paste it by hand.",
                  "//",
                  "// It goes into the ENVIRONMENT when one is selected, because a",
                  "// collection variable is only visible to the collection that",
                  "// set it -- log in once in `user` and the other 17 collections",
                  "// would still 401. Select the KairoX environment and one login",
                  "// authorises all of them. The collection variable is written",
                  "// too, so this still works with no environment selected.",
                  "if (pm.response.code === 200) {",
                  "    const body = pm.response.json();",
                  "    if (body.access_token) {",
                  "        pm.collectionVariables.set('token', body.access_token);",
                  "        if (pm.environment.name) {",
                  "            pm.environment.set('token', body.access_token);",
                  "        }",
                  "        console.log('token stored'",
                  "            + (pm.environment.name",
                  "               ? ' in environment ' + pm.environment.name",
                  "               : ' in this collection only -- select the KairoX'",
                  "                 + ' environment to share it'));",
                  "    }",
                  "}",
                  "pm.test('login returns 200 + access_token', function () {",
                  "    pm.response.to.have.status(200);",
                  "    pm.expect(pm.response.json()).to.have.property('access_token');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Me",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/api/v1/auth/me",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "v1",
                "auth",
                "me"
              ]
            },
            "description": ""
          },
          "response": []
        }
      ]
    },
    {
      "name": "Users",
      "item": [
        {
          "name": "Create User",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/api/v1/users",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "v1",
                "users"
              ]
            },
            "description": "B8: the role gate admits DM/HR/MD, but WHICH role may be granted is\ndecided in the service against the caller's own authority.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"\",\n  \"phone\": \"\",\n  \"email\": \"\",\n  \"role\": \"managing_director\",\n  \"password\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "List Users",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/api/v1/users",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "api",
                "v1",
                "users"
              ],
              "query": [
                {
                  "key": "active_only",
                  "value": "True",
                  "description": "",
                  "disabled": true
                },
                {
                  "key": "limit",
                  "value": "50",
                  "description": "Rows to return (1-200).",
                  "disabled": false
                },
                {
                  "key": "offset",
                  "value": "0",
                  "description": "Rows to skip before returning `limit` rows.",
                  "disabled": false
                }
              ]
            },
            "description": "The login roster. PAGED \u2014 see core/pagination.py."
          },
          "response": []
        }
      ]
    }
  ]
}