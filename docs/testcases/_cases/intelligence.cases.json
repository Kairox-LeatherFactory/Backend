{
  "service": "Intelligence (Chatbot)",
  "slug": "intelligence",
  "module": "app/modules/intelligence",
  "stage": "Chatbot — deterministic agent with an optional LLM path",
  "prefix": "/api/v1",
  "intro": "Request-level test cases for the chatbot API: a JSON question/answer endpoint and an SSE streaming variant. Both run the same agent — a keyword intent router over four production tools (schedule status, bottleneck, plan, overview) — so most of the testable behaviour is intent routing, degradation and auth rather than status codes.",
  "notes": [
    "**No role gate at all.** Both endpoints depend only on `get_current_user`, so every logged-in role reaches them. The only auth case that fails is a missing or invalid token (401). Whether a Viewer should be able to ask for factory-wide numbers is a product question worth raising.",
    "**Intent routing is keyword matching, in a fixed order**: bottleneck words → plan words → overview words → schedule (default when a style is named). The first matching branch wins, so a question containing two intents resolves to whichever appears earlier in that chain, not to the more specific one.",
    "The literal word **\"leather\"** is in the *planning* keyword list. Any question mentioning leather routes to the planner — see A-L3.",
    "`use_llm: true` only reaches the LangGraph agent when `CHAT_MODEL` is configured; otherwise the request silently falls back to the deterministic router. The response shape is identical either way, so assert on behaviour, not on which engine ran.",
    "`/chat/stream` computes the **whole** answer first and then chunks it word by word for a typing effect. Time-to-first-token therefore equals full compute time — it is not a real token stream (B-L1).",
    "`ChatRequest.question` has no length bound and no minimum; empty and enormous strings are both accepted (A-B1, A-B2)."
  ],
  "sections": [
    {
      "code": "A",
      "title": "Ask a question",
      "sub": "POST /chat",
      "gate": "Any authenticated user",
      "cases": [
        {"id": "A-P1", "cat": "POS", "prio": "High", "status": "200", "title": "Schedule status for a named style", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat", "Body: {\"question\":\"Is CARNABY on schedule?\",\"use_llm\":false}"],
          "expected": "200 {\"answer\":\"<headline + advice>\",\"tool\":\"schedule_status\",\"data\":{ok, required rate, projected finish, …}} — substitute a style name that exists in your seed data."},
        {"id": "A-P2", "cat": "POS", "prio": "High", "status": "200", "title": "Bottleneck question", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"Where is the bottleneck right now?\"}"],
          "expected": "200 with tool \"bottleneck\" and data naming the slowest stage/department plus a note. Try the synonyms too — \"which department is lagging\", \"what is slowing production\" — they all route here."},
        {"id": "A-P3", "cat": "POS", "prio": "High", "status": "200", "title": "Planning with a blocked style", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat", "Body: {\"question\":\"Plan production, RICANO leather is late\"}"],
          "expected": "200 with tool \"plan\"; the named style is passed as a blocked style, so the returned daily targets route capacity to the other styles. The answer text should say which style was held back."},
        {"id": "A-P4", "cat": "POS", "prio": "Medium", "status": "200", "title": "Factory overview", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"Give me an overview of the factory\"}"],
          "expected": "200 with tool \"overview\" and data carrying clients, styles, total ordered and total produced."},
        {"id": "A-P5", "cat": "POS", "prio": "Medium", "status": "200", "title": "A daily capacity figure is picked up", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"What rate do we need for CARNABY if we can do 40 a day?\"}"],
          "expected": "200; the parsed capacity (40/day) constrains the answer — the router extracts \"N a day\" / \"N per day\" / \"N jackets a day\" and passes it to the tool as max_daily."},
        {"id": "A-P6", "cat": "POS", "prio": "Low", "status": "200", "title": "Every logged-in role can ask", "role": "Office Viewer (9000000004), then Security Gate (9000000007)",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"Give me an overview\"} as each role"],
          "expected": "200 for both — there is no role gate. Note in your report whether factory-wide numbers should really be readable by a gate/security login."},
        {"id": "A-N1", "cat": "NEG", "prio": "High", "status": "401", "title": "No token", "role": "Unauthenticated",
          "steps": ["POST /api/v1/chat with no Authorization header — Body: {\"question\":\"overview\"}"],
          "expected": "401 {\"detail\":\"Could not validate credentials\"} with a WWW-Authenticate: Bearer header."},
        {"id": "A-N2", "cat": "NEG", "prio": "High", "status": "422", "title": "Missing the question field", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {}", "then Body: {\"use_llm\":true}"],
          "expected": "422 both times — `question` is required on ChatRequest."},
        {"id": "A-N3", "cat": "NEG", "prio": "Medium", "status": "422", "title": "Wrong types", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\": 123}", "then Body: {\"question\":\"overview\",\"use_llm\":\"yes\"}"],
          "expected": "The first 422s (int is not a string under strict coercion — confirm against your Pydantic settings). The second may be accepted, since \"yes\" is a truthy string for bool coercion — record which."},
        {"id": "A-N4", "cat": "NEG", "prio": "Low", "status": "401", "title": "Expired or garbage token", "role": "Any role with a bad token",
          "steps": ["POST /api/v1/chat with Authorization: Bearer not.a.real.token"],
          "expected": "401 — the token is decoded before the user lookup, so a malformed token fails the same way an expired one does."},
        {"id": "A-B1", "cat": "BND", "prio": "Medium", "status": "200", "title": "Empty question", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"\"}"],
          "expected": "200 with the fallback answer (\"I can answer questions about production schedules, bottlenecks, and planning…\"), tool null and data.styles listing up to 8 known styles. No 422 — there is no min_length on the field."},
        {"id": "A-B2", "cat": "BND", "prio": "Medium", "status": "200", "title": "Very long question", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"<10,000 characters of text>\"}"],
          "expected": "200, no crash and no timeout — the whole string is lowercased and keyword-scanned. Flag the absence of any length bound: this is the cheapest way to make the endpoint do work."},
        {"id": "A-B3", "cat": "BND", "prio": "Medium", "status": "200", "title": "Unknown style name", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"Is NOTASTYLE on schedule?\"}"],
          "expected": "200 — schedule words are present but no known style matches, so the answer asks which style you mean and data.styles offers real names. Never a 404 or a 500."},
        {"id": "A-B4", "cat": "BND", "prio": "Low", "status": "200", "title": "Style name in the wrong case or padded", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"is carnaby on schedule\"}  (lowercase, no punctuation)"],
          "expected": "200 with tool schedule_status — matching is done on a lowercased question, so case and trailing punctuation must not matter. If it fails here, the style matcher is stricter than the router and that is worth a bug."},
        {"id": "A-L1", "cat": "LGC", "prio": "High", "status": "200", "title": "Ambiguity produces a question, not a guess", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"are we late?\"}"],
          "expected": "200; schedule words matched but no style was named, so the answer is a clarifying question naming an example style, with tool null and data.styles populated. The agent must not pick a style on the user's behalf."},
        {"id": "A-L2", "cat": "LGC", "prio": "High", "status": "200", "title": "use_llm with no model configured degrades silently", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"Where is the bottleneck?\",\"use_llm\":true}", "[in an environment where CHAT_MODEL is unset]"],
          "expected": "200 with the same deterministic answer as A-P2 — never a 500, never a hang, and no error field telling the caller the LLM was skipped. Confirm; a caller cannot currently tell which engine answered."},
        {"id": "A-L3", "cat": "LGC", "prio": "High", "status": "200", "title": "\"leather\" hijacks the question into the planner", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"How much leather do we have in stock?\"}"],
          "expected": "200 with tool \"plan\" — \"leather\" is a planning keyword, so a stock question is answered with a production plan. The routing is keyword-order based, not intent-based. Confirmed quirk; file it with this exact example."},
        {"id": "A-L4", "cat": "LGC", "prio": "Medium", "status": "200", "title": "First matching branch wins", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"Give me an overview of the bottleneck\"}"],
          "expected": "tool \"bottleneck\", not \"overview\" — bottleneck is checked first. Worth documenting for anyone writing UI suggestion chips, so the chips don't collide."},
        {"id": "A-L5", "cat": "LGC", "prio": "Medium", "status": "200", "title": "The fallback teaches the user", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"what is the weather\"}"],
          "expected": "200 with the fallback answer listing the three things it can actually do, tool null, and real style names in data.styles — an unrecognised question must be a helpful dead end, not an error."},
        {"id": "A-L6", "cat": "LGC", "prio": "Low", "status": "200", "title": "The answer and the data agree", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat — Body: {\"question\":\"Is CARNABY on schedule?\"}", "Compare the prose in `answer` against the numbers in `data`"],
          "expected": "The rendered sentence is built from the same tool result it returns, so the headline verdict, the rate and the dates in the prose must match `data` exactly. A mismatch means a renderer bug and would make the chatbot untrustworthy."}
      ]
    },
    {
      "code": "B",
      "title": "Streaming answers",
      "sub": "POST /chat/stream",
      "gate": "Any authenticated user",
      "cases": [
        {"id": "B-P1", "cat": "POS", "prio": "High", "status": "200", "title": "Consume the SSE stream", "role": "Any authenticated role",
          "steps": ["curl -N -X POST {{base_url}}/api/v1/chat/stream -H \"Authorization: Bearer {token}\" -H \"Content-Type: application/json\" -d '{\"question\":\"Where is the bottleneck?\"}'"],
          "expected": "200 with content-type text/event-stream: a sequence of `data: {\"delta\":\"<word> \"}` frames, then a final `data: {\"done\":true,\"tool\":\"bottleneck\",\"data\":{…}}` frame. The -N flag is required or curl buffers the whole response."},
        {"id": "B-P2", "cat": "POS", "prio": "Medium", "status": "200", "title": "The streamed text equals the JSON answer", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat with a question, note `answer`", "Stream the same question through /api/v1/chat/stream and concatenate every delta"],
          "expected": "The concatenated deltas reproduce the JSON endpoint's `answer` (allowing for the trailing space on each word), and the final frame's tool/data match. The two endpoints must never diverge."},
        {"id": "B-N1", "cat": "NEG", "prio": "High", "status": "401", "title": "No token on the stream", "role": "Unauthenticated",
          "steps": ["curl -N -X POST {{base_url}}/api/v1/chat/stream with no Authorization header"],
          "expected": "401 before any stream opens — the dependency runs first, so you get a normal JSON error, not an event-stream containing an error."},
        {"id": "B-N2", "cat": "NEG", "prio": "Medium", "status": "422", "title": "Missing question on the stream", "role": "Any authenticated role",
          "steps": ["POST /api/v1/chat/stream — Body: {}"],
          "expected": "422, again as a normal JSON response rather than an SSE frame."},
        {"id": "B-B1", "cat": "BND", "prio": "Medium", "status": "200", "title": "Empty question streams the fallback", "role": "Any authenticated role",
          "steps": ["Stream Body: {\"question\":\"\"}"],
          "expected": "200; the fallback sentence arrives word by word and the final frame carries tool null with data.styles. Confirm the stream terminates rather than hanging on an empty answer."},
        {"id": "B-B2", "cat": "BND", "prio": "Low", "status": "200", "title": "Client disconnects mid-stream", "role": "Any authenticated role",
          "steps": ["Start the stream and kill the client after the first few frames", "Check the server log for an unhandled exception"],
          "expected": "The generator stops cleanly; no traceback, no stuck worker. The whole answer was already computed before the first frame, so nothing is left half-done server-side."},
        {"id": "B-L1", "cat": "LGC", "prio": "High", "status": "200", "title": "It is a fake stream, and that shows in the timing", "role": "Any authenticated role",
          "steps": ["Time the first delta frame of /chat/stream", "Time the full response of /chat for the same question"],
          "expected": "Time-to-first-token is roughly the FULL compute time, not a fraction of it — the endpoint computes the whole answer then chunks it at ~20 ms per word. A slow tool therefore looks like a frozen UI, not a slow typist. Document it; this is the thing to change when a real LLM is wired in."},
        {"id": "B-L2", "cat": "LGC", "prio": "Medium", "status": "200", "title": "Postman's plain Send is the wrong tool here", "role": "Any authenticated role",
          "steps": ["Send /api/v1/chat/stream from Postman's normal request view"],
          "expected": "The request appears to hang or buffers until the whole stream ends. That is a client limitation, not an API defect — use curl -N or a client with SSE support. Same caveat as the BOM module's notification stream."}
      ]
    }
  ]
}
